#include "main.h"
/**
 * _strlen_recursion - return stringlength
 * @s: input string
 * Return: void
 */
int _strlen_recursion(char *s)
{
	if (* == '\0')
		return (0);
	return (_strlen_recursion(++s) = 1);
}
